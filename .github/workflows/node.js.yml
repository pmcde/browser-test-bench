# This workflow will do a clean install of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on: push

jobs:
  Scan:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.SCAN_PAT }}
      CONTRAST__API__USER_NAME: "service-account@contrast.com"
      CONTRAST__API__ORGANIZATION_ID: "5deb031c-8a39-4d16-bc9f-f3736024b3b5"
      CONTRAST__API__SERVICE_KEY: "U8DTANW8ECFC93RA"
      CONTRAST__API__API_KEY: "iTykB1I11E9f467RpT32Wo7UBHFdi6g2"
    steps:
      - uses: actions/checkout@v2
      - name: Install and Build
        run: npm run package
      - name: Contrast Scan
        uses: Contrast-Security-OSS/contrastscan-action@v1
        with:
          artifact: param.war
          api_url: https://teamserver-staging.contsec.com/Contrast

  local-scan:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.SCAN_PAT }}
    steps:
      - uses: actions/checkout@v3
      - uses: Contrast-Security-OSS/contrast-local-scan-action@v1.0.1
        with:
          apiUrl: "https://teamserver-harmony-dev.contsec.com/Contrast/api/sast"
          apiUserName: "patrick.mcdevitt@contrastsecurity.com"
          apiKey: "LfXlest12DW34r7Gt4wSj4c6YYkEUMqc"
          apiServiceKey: "WT7TDX0M4AE8MRL"
          apiOrgId: "74f4cd04-6ca9-4eb7-a7a7-78909c2101cc"
