let msg = "Hash Value: " + window.location.hash.substring(1) + " Cookie: " + document.cookie;
document.cookie = "Custom=" + window.location.hash.substring(1);

document.getElementById("output").innerText = msg;