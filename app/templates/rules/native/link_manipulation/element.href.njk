let hashvalue = window.location.hash.substring(1);
let msg = `Hash value: ${hashvalue} <br><a id="link" href="element.href.html"> Link </a>`;

document.getElementById("output").innerHTML = msg;

if(hashvalue){
    document.getElementById("link").href = hashvalue;
}
