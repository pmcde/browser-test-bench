let hashvalue = window.location.hash.substring(1);

let msg = `Hash value: ${hashvalue}\n`;

// e.g.: hashvalue = output
if(hashvalue){
    var result = document.evaluate('//*[@id="'+ hashvalue + '"]', document, null, XPathResult.ANY_TYPE, null);   
    var node, nodes = []
    while (node = result.iterateNext())
        nodes.push(node);

    console.log(nodes);
}

// Some trusted logic using the attackers query result

document.getElementById("output").innerText = msg;
