let hashvalue = window.location.hash.substring(1);
let msg = `Hash value: ${hashvalue}\n`;
localStorage.setItem('key','hardcoded value');
if(hashvalue){
  localStorage.setItem('key', hashvalue);
}

document.getElementById("output").innerText = msg;
